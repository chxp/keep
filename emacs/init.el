;; (require 'package)
;; (package-initialize)

;; (require 'org)
;; (require 'ob-tangle)
;; ;; 可以在 *scratch* Buffer 中运行此句，随后只需加载 _emacs.el
;; (org-babel-load-file (expand-file-name "~/syncthing/emacs/_emacs.org"))

(load-file "~/syncthing/emacs/_emacs.el")
